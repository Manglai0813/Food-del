# 02-アーキテクチャ設計

> Food-Del-Dashboard の全体アーキテクチャと設計思想

**最終更新**: 2025-10-15  
**対象読者**: アーキテクト、シニア開発者

---

## 📋 目次

1. [アーキテクチャ概要](#アーキテクチャ概要)
2. [7層アーキテクチャ](#7層アーキテクチャ)
3. [Barrel Export パターン](#barrel-exportパターン)
4. [データフロー設計](#データフロー設計)
5. [依存関係管理](#依存関係管理)
6. [スケーラビリティ設計](#スケーラビリティ設計)

---

## 🏗 アーキテクチャ概要

### 設計原則

Food-Del-Dashboardは、以下の設計原則に基づいて構築されています：

```
1️⃣ 関心の分離 (Separation of Concerns)
   - 各層は明確な責任を持つ
   - 層間の依存は一方向のみ

2️⃣ 疎結合 (Loose Coupling)
   - モジュール間の依存を最小化
   - インターフェースを介した通信

3️⃣ 高凝集 (High Cohesion)
   - 関連する機能を同じモジュールに集約
   - 単一責任の原則

4️⃣ 再利用性 (Reusability)
   - 汎用コンポーネントの設計
   - Barrel Exportによる統一API

5️⃣ テスト容易性 (Testability)
   - ビジネスロジックの分離
   - 依存性注入の活用
```

### アーキテクチャ図

```
┌───────────────────────────────────────────────────────────────┐
│  Entry Layer - エントリー層                                    │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ main.tsx → App.tsx → React Router v7                     │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  Pages Layer - ページ層                                       │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ auth/  dashboard/  foods/  orders/  categories/          │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  Components Layer - コンポーネント層                           │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ common/  dashboard/  layout/  ui/                        │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  Business Logic Layer - ビジネスロジック層                     │
│  ┌─────────────┬─────────────┬─────────────┐                 │
│  │ hooks/      │ stores/     │ configs/    │                 │
│  └─────────────┴─────────────┴─────────────┘                 │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  API Layer - API通信層 (TanStack Query v5)                     │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ authApi  foodsApi  categoriesApi  ordersApi  dashboardApi│ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  HTTP Client Layer - HTTPクライアント層                        │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ httpClient.ts (Axios + Interceptors)                     │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘
                           ↓
┌───────────────────────────────────────────────────────────────┐
│  Backend API - バックエンドAPI層                               │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │ food-del-server (Bun + Prisma + PostgreSQL)              │ │
│  └──────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────┘

         ┌────────────────────────────────────┐
         │  Support Layer - 補助層             │
         │  ┌──────────┬──────────┬─────────┐ │
         │  │ types/   │ lib/     │ docs/   │ │
         │  └──────────┴──────────┴─────────┘ │
         └────────────────────────────────────┘
              （横断的関心事）
```

---

## 🏛 7層アーキテクチャ

### Layer 1: Entry Layer（エントリー層）

**責任**:
- アプリケーションの初期化
- ルーティング設定
- グローバルプロバイダー設定

**主要ファイル**:
```typescript
// main.tsx - アプリケーションエントリーポイント
import React from 'react';
import ReactDOM from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import { QueryClientProvider } from '@tanstack/react-query';
import { queryClient } from '@/lib/queryClient';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BrowserRouter>
      <QueryClientProvider client={queryClient}>
        <App />
      </QueryClientProvider>
    </BrowserRouter>
  </React.StrictMode>
);
```

```typescript
// App.tsx - ルーティング設定
import { Routes, Route, Navigate } from 'react-router-dom';
import { Layout } from '@/components/layout';
import { LoginPage } from '@/pages/auth';
import { DashboardPage } from '@/pages/dashboard';
import { ProtectedRoute } from '@/components/common';

function App() {
  const { isAuthenticated } = useAuth();
  
  return (
    <Routes>
      <Route path="/login" element={<LoginPage />} />
      <Route path="/*" element={
        <ProtectedRoute>
          <Layout />
        </ProtectedRoute>
      }>
        <Route index element={<DashboardPage />} />
        {/* 他のルート */}
      </Route>
    </Routes>
  );
}
```

### Layer 2: Pages Layer（ページ層）

**責任**:
- ページレベルの状態管理
- コンポーネントの組み合わせ
- データフェッチの開始

**設計パターン**:
```typescript
// pages/dashboard/DashboardPage.tsx
const DashboardPage = () => {
  // 🎣 データフェッチ（TanStack Query）
  const { data: orderStats, isLoading } = useOrderStats();
  const { data: overview } = useDashboardOverview();
  
  // 🔄 自動更新設定
  useDashboardAutoRefresh();
  
  // 🎨 UIレンダリング（コンポーネント組み合わせ）
  return (
    <div className="space-y-6">
      <StatsCard data={orderStats} />
      <SalesChart data={overview?.salesData} />
      <OrderStatusChart data={orderStats?.statusBreakdown} />
    </div>
  );
};
```

**ページ層の責務**:
- ✅ ビジネスロジックフックの呼び出し
- ✅ コンポーネントのレイアウト決定
- ❌ 直接のAPI呼び出し（API層に委譲）
- ❌ 複雑な状態ロジック（hooks層に委譲）

### Layer 3: Components Layer（コンポーネント層）

**責任**:
- UIの描画
- ユーザーインタラクション処理
- プレゼンテーションロジック

**コンポーネント分類**:

#### 3.1 common/ - 汎用コンポーネント

```typescript
// DataTable.tsx - 設定駆動テーブル
interface DataTableProps<T> {
  data: T[];                    // 表示データ
  columns: ColumnDef<T>[];      // 列定義
  onEdit?: (row: T) => void;    // 編集ハンドラー
  onDelete?: (row: T) => void;  // 削除ハンドラー
  isLoading?: boolean;          // ローディング状態
}

export function DataTable<T>({ data, columns, ... }: DataTableProps<T>) {
  // テーブルロジック
}
```

#### 3.2 dashboard/ - ダッシュボード専用コンポーネント

```typescript
// StatsCard.tsx - 統計カード
interface StatsCardProps {
  title: string;
  value: number | string;
  change?: number;              // 前日比
  trend?: 'up' | 'down' | 'stable';
  icon?: React.ReactNode;
  loading?: boolean;
}

const StatsCard: React.FC<StatsCardProps> = ({ ... }) => {
  // カードUIレンダリング
}
```

#### 3.3 layout/ - レイアウトコンポーネント

```typescript
// Layout.tsx - メインレイアウト
const Layout = () => {
  const [sidebarCollapsed, setSidebarCollapsed] = useState(false);
  
  return (
    <div className="flex h-screen">
      <Sidebar collapsed={sidebarCollapsed} />
      <div className="flex-1 flex flex-col">
        <Header onToggleSidebar={() => setSidebarCollapsed(!sidebarCollapsed)} />
        <main className="flex-1 overflow-auto">
          <Outlet />  {/* React Router のネストされたルート */}
        </main>
      </div>
    </div>
  );
};
```

### Layer 4: Business Logic Layer（ビジネスロジック層）

**責任**:
- ビジネスルールの実装
- 状態管理ロジック
- 設定管理

#### 4.1 hooks/ - カスタムフック

```typescript
// useAuth.ts - 認証状態管理
export const useAuth = () => {
  const { user, accessToken, isAuthenticated } = useAuthStore();
  const loginMutation = useLogin();
  const logoutMutation = useLogout();
  
  const login = async (credentials: LoginRequest) => {
    const response = await loginMutation.mutateAsync(credentials);
    // ログイン後の処理
  };
  
  return {
    user,
    isAuthenticated,
    login,
    logout: logoutMutation.mutate,
  };
};
```

#### 4.2 stores/ - グローバル状態（Zustand）

```typescript
// authStore.ts - 認証グローバル状態
interface AuthState {
  user: User | null;
  accessToken: string | null;
  refreshToken: string | null;
  isAuthenticated: boolean;
  login: (tokens: Tokens, user: User) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      refreshToken: null,
      isAuthenticated: false,
      
      login: (tokens, user) => set({
        user,
        accessToken: tokens.accessToken,
        refreshToken: tokens.refreshToken,
        isAuthenticated: true,
      }),
      
      logout: () => set({
        user: null,
        accessToken: null,
        refreshToken: null,
        isAuthenticated: false,
      }),
    }),
    { name: 'auth-storage' }
  )
);
```

#### 4.3 configs/ - 設定管理

```typescript
// products.ts - 商品エンティティ設定
export const productEntityConfig = {
  name: '商品',
  fields: [
    { name: 'name', label: '商品名', type: 'text', required: true },
    { name: 'price', label: '価格', type: 'number', required: true },
    { name: 'category_id', label: 'カテゴリ', type: 'select', required: true },
    { name: 'image', label: '画像', type: 'file', accept: 'image/*' },
  ],
  table: {
    columns: ['id', 'name', 'price', 'category', 'status'],
    actions: ['view', 'edit', 'delete'],
  },
};
```

### Layer 5: API Layer（API通信層）

**責任**:
- サーバーとの通信
- データフェッチ
- キャッシュ管理

**TanStack Query パターン**:

```typescript
// foodsApi.ts
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import httpClient from '@/lib/httpClient';

// 商品一覧取得
export const useFoods = (query?: FoodSearchQuery) => {
  return useQuery({
    queryKey: ['foods', query],          // キャッシュキー
    queryFn: async () => {
      const response = await httpClient.get('/api/foods/list', { params: query });
      return response.data;
    },
    staleTime: 5 * 60 * 1000,            // 5分間キャッシュ有効
  });
};

// 商品作成
export const useCreateFood = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (data: CreateFoodRequest) => {
      const formData = new FormData();
      Object.entries(data).forEach(([key, value]) => {
        formData.append(key, value);
      });
      
      const response = await httpClient.post('/api/foods/create', formData);
      return response.data;
    },
    onSuccess: () => {
      // キャッシュ無効化（自動再フェッチ）
      queryClient.invalidateQueries({ queryKey: ['foods'] });
    },
  });
};
```

### Layer 6: HTTP Client Layer（HTTPクライアント層）

**責任**:
- HTTP通信の抽象化
- リクエスト/レスポンスインターセプター
- トークン管理

```typescript
// httpClient.ts
import axios from 'axios';
import { useAuthStore } from '@/stores/authStore';

const httpClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || 'http://localhost:5000',
  timeout: 10000,
});

// リクエストインターセプター - JWT付与
httpClient.interceptors.request.use(
  (config) => {
    const { accessToken } = useAuthStore.getState();
    if (accessToken) {
      config.headers.Authorization = `Bearer ${accessToken}`;
    }
    return config;
  },
  (error) => Promise.reject(error)
);

// レスポンスインターセプター - トークン更新
httpClient.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;
    
    // 401エラー時にトークン更新試行
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;
      
      try {
        const { refreshToken } = useAuthStore.getState();
        const response = await axios.post('/api/auth/refresh', { refreshToken });
        
        // 新しいトークンで再試行
        useAuthStore.getState().login(response.data.tokens, response.data.user);
        return httpClient(originalRequest);
      } catch (refreshError) {
        // リフレッシュ失敗 → ログアウト
        useAuthStore.getState().logout();
        window.location.href = '/login';
        return Promise.reject(refreshError);
      }
    }
    
    return Promise.reject(error);
  }
);

export default httpClient;
```

### Layer 7: Support Layer（補助層）

**横断的関心事**を処理：

#### types/ - 型定義

```typescript
// food.ts
export interface Food {
  id: number;
  name: string;
  description: string;
  price: number;
  category_id: number;
  image_path: string;
  status: boolean;
  stock: number;
  reserved: number;
  category: Category;  // リレーション
}
```

#### lib/ - ユーティリティ

```typescript
// utils.ts
export const formatCurrency = (amount: number): string => {
  return new Intl.NumberFormat('ja-JP', {
    style: 'currency',
    currency: 'JPY',
  }).format(amount);
};
```

---

## 📦 Barrel Export パターン

### 設計思想

Barrel Exportパターンにより、以下を実現：

1. **統一されたAPI** - 各モジュールが単一のエントリーポイントを持つ
2. **保守性向上** - インポート先の変更が容易
3. **可読性向上** - import文が簡潔になる

### 実装パターン

```
src/api/
├── index.ts          # 🌟 Barrel Export
├── authApi.ts
├── foodsApi.ts
└── ...
```

```typescript
// src/api/index.ts - Barrel Export
export * from './authApi';
export * from './foodsApi';
export * from './categoriesApi';
export * from './ordersApi';
export * from './dashboardApi';
```

### 使用例

```typescript
// ❌ Before - 個別インポート
import { useLogin } from '@/api/authApi';
import { useFoods } from '@/api/foodsApi';
import { useCategories } from '@/api/categoriesApi';

// ✅ After - Barrel Export
import { useLogin, useFoods, useCategories } from '@/api';
```

### Named Export vs Default Export

```typescript
// Named Export（推奨） - Barrel Exportと相性が良い
export const useAuth = () => { ... };
export * from './useAuth';  // index.ts で再エクスポート容易

// Default Export - Barrel Exportで別名が必要
export default function AuthComponent() { ... }
export { default as AuthComponent } from './AuthComponent';  // 明示的に
```

---

## 🔄 データフロー設計

### 認証フロー

```
┌──────────────┐
│  LoginPage   │
└──────┬───────┘
       │ ユーザー入力
       ↓
┌──────────────────┐
│ useLogin()       │ (authApi.ts)
│ - POST /api/auth/login
└──────┬───────────┘
       │ tokens + user
       ↓
┌──────────────────────┐
│ authStore.login()    │ (Zustand)
│ - localStorage保存
└──────┬───────────────┘
       │
       ↓
┌──────────────────────┐
│ Navigate('/')        │ → Dashboard
└──────────────────────┘
```

### Dashboard統計データフロー

```
┌─────────────────┐
│ DashboardPage   │
└──────┬──────────┘
       │
       ├─→ useOrderStats()
       ├─→ useDashboardOverview()
       └─→ useDashboardSales()
       │
       │ TanStack Query (5分キャッシュ)
       ↓
┌──────────────────────────┐
│ Parallel API Calls       │
│ - GET /api/orders/admin/stats
│ - GET /api/categories/list
│ - GET /api/foods/list
└──────┬───────────────────┘
       │ 生データ
       ↓
┌──────────────────────────┐
│ データ変換層              │
│ - transformToOverviewStats()
│ - transformToSalesStats()
└──────┬───────────────────┘
       │ UI用データ
       ↓
┌──────────────────────────┐
│ コンポーネントレンダリング │
│ - StatsCard
│ - SalesChart
│ - OrderStatusChart
└──────────────────────────┘
```

### CRUD操作フロー（商品作成例）

```
┌─────────────────┐
│ FoodFormPage    │ (入力フォーム)
└──────┬──────────┘
       │ onSubmit
       ↓
┌──────────────────┐
│ useCreateFood()  │ (foodsApi.ts)
└──────┬───────────┘
       │ FormData作成
       ↓
┌──────────────────────┐
│ POST /api/foods/create
│ Content-Type: multipart/form-data
└──────┬───────────────┘
       │ Success
       ↓
┌──────────────────────────┐
│ onSuccess コールバック     │
│ - invalidateQueries(['foods'])
└──────┬───────────────────┘
       │ キャッシュ無効化
       ↓
┌──────────────────────────┐
│ TanStack Query           │
│ - 自動再フェッチ          │
│ - useFoods() 更新        │
└──────┬───────────────────┘
       │ 最新データ
       ↓
┌──────────────────────────┐
│ FoodListPage             │
│ - テーブル自動更新        │
└──────────────────────────┘
```

---

## 🔗 依存関係管理

### 依存関係ルール

```
Pages → Components → Hooks/Stores → API → HTTP Client
  ↓           ↓           ↓          ↓         ↓
Types ←──────────────────────────────────────┘
```

**原則**:
- ✅ 上位層は下位層に依存できる
- ❌ 下位層は上位層に依存できない
- ✅ 同じ層内での依存は最小化
- ✅ Types層は全層から参照可能（横断的関心事）

### Import パス管理

```typescript
// tsconfig.json - パスエイリアス設定
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

**インポート規約**:
```typescript
// ✅ Good - パスエイリアス使用
import { useAuth } from '@/hooks';
import { Food } from '@/types';

// ❌ Bad - 相対パス
import { useAuth } from '../../../hooks/useAuth';
```

---

## 📈 スケーラビリティ設計

### 横方向スケーラビリティ

新機能追加時の拡張性：

```
新機能「在庫管理」を追加する場合：

1. types/inventory.ts          - 型定義追加
2. api/inventoryApi.ts         - API追加
3. pages/inventory/*           - ページ追加
4. components/inventory/*      - 専用コンポーネント追加
5. App.tsx                     - ルート追加

既存コードへの影響 = 最小限
```

### 縦方向スケーラビリティ

パフォーマンス最適化：

```typescript
// コード分割（動的インポート）
const DashboardPage = lazy(() => import('@/pages/dashboard'));
const OrderListPage = lazy(() => import('@/pages/orders'));

// Suspense でローディング管理
<Suspense fallback={<Loading />}>
  <DashboardPage />
</Suspense>
```

### キャッシュ戦略

```typescript
// TanStack Query 設定
export const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 5 * 60 * 1000,      // 5分間キャッシュ有効
      cacheTime: 10 * 60 * 1000,     // 10分間メモリ保持
      refetchOnWindowFocus: true,    // フォーカス時再フェッチ
      retry: 3,                      // 失敗時3回リトライ
    },
  },
});
```

---

## 📝 まとめ

このアーキテクチャにより、以下を実現：

- ✅ **保守性** - 明確な層分離により変更が容易
- ✅ **テスト容易性** - ビジネスロジックの分離
- ✅ **拡張性** - 新機能追加時の影響範囲が限定的
- ✅ **パフォーマンス** - キャッシュ戦略による高速化
- ✅ **開発者体験** - Barrel Exportによる簡潔なコード

---

**次のドキュメント**: [03-開発ガイド.md](./03-開発ガイド.md)

