# 05-コンポーネント設計

> Food-Del-Dashboard のコンポーネント構造と設計パターン

## 📋 目次

1. [コンポーネント階層構造](#コンポーネント階層構造)
2. [共通コンポーネント](#共通コンポーネント)
3. [ダッシュボードコンポーネント](#ダッシュボードコンポーネント)
4. [レイアウトコンポーネント](#レイアウトコンポーネント)
5. [UIコンポーネント](#uiコンポーネント)
6. [コンポーネント合成パターン](#コンポーネント合成パターン)
7. [スタイリング戦略](#スタイリング戦略)

---

## 1. コンポーネント階層構造

### 1.1 全体アーキテクチャ

```
src/
├── components/
│   ├── common/           【共通コンポーネント層】
│   │   ├── DataTable.tsx         ← 汎用データテーブル（設定駆動）
│   │   ├── DynamicForm.tsx       ← 汎用フォーム（設定駆動）
│   │   ├── ProtectedRoute.tsx    ← 認証・認可ガード
│   │   ├── ActionMenu.tsx        ← 編集・削除アクションメニュー
│   │   ├── GlobalSearch.tsx      ← グローバル検索
│   │   ├── ColumnFilter.tsx      ← テーブルフィルタ
│   │   ├── FormField.tsx         ← フォームフィールド
│   │   └── index.ts              ← Barrel Export
│   │
│   ├── dashboard/        【ダッシュボード専用コンポーネント】
│   │   ├── StatsCard.tsx         ← 統計カード（数値表示）
│   │   ├── SalesChart.tsx        ← 売上推移チャート
│   │   ├── OrderStatusChart.tsx  ← 注文ステータス円グラフ
│   │   ├── OrdersByHourChart.tsx ← 時間帯別注文グラフ
│   │   ├── CategoryChart.tsx     ← カテゴリ別売上グラフ
│   │   ├── TopProductsList.tsx   ← 人気商品ランキング
│   │   └── index.ts
│   │
│   ├── layout/           【レイアウトコンポーネント】
│   │   ├── Layout.tsx            ← メインレイアウト（Header + Sidebar + Content）
│   │   ├── Header.tsx            ← ヘッダー（検索・通知・ユーザーメニュー）
│   │   ├── Sidebar.tsx           ← サイドバー（ナビゲーション）
│   │   └── index.ts
│   │
│   └── ui/               【基礎UIコンポーネント（shadcn/ui）】
│       ├── button.tsx
│       ├── card.tsx
│       ├── input.tsx
│       ├── table.tsx
│       ├── select.tsx
│       ├── textarea.tsx
│       ├── badge.tsx
│       ├── checkbox.tsx
│       ├── dropdown-menu.tsx
│       ├── label.tsx
│       ├── avatar.tsx
│       └── progress.tsx
```

### 1.2 依存関係ルール

```
Pages
  ↓ 依存OK
Components (common, dashboard, layout)
  ↓ 依存OK
UI Components (shadcn/ui)
  ↓ 依存OK
React基本フック


❌ 禁止: 下位層から上位層への依存
例: ui → common へのインポート
例: common → pages へのインポート
```

---

## 2. 共通コンポーネント

### 2.1 DataTable (汎用データテーブル)

#### 概要
設定駆動型のデータテーブルコンポーネント。ページネーション、ソート、フィルタ、アクション機能を標準装備。

#### Props定義
```typescript
/**
 * DataTable.tsx
 * 
 * 汎用データテーブルコンポーネント
 * - 設定ベースのカラム定義
 * - ページネーション対応
 * - ソート・フィルタ機能
 * - 行アクション（編集・削除）
 */

import type { ColumnDef } from '@/types/crud-config';

interface DataTableProps<T> {
  data: T[];                              // テーブルデータ
  columns: ColumnDef<T>[];                // カラム定義
  pagination?: {
    currentPage: number;
    totalPages: number;
    totalItems: number;
    onPageChange: (page: number) => void;
  };
  onEdit?: (row: T) => void;              // 編集ハンドラ
  onDelete?: (row: T) => void;            // 削除ハンドラ
  onRowClick?: (row: T) => void;          // 行クリックハンドラ
  isLoading?: boolean;
  emptyMessage?: string;
}

export function DataTable<T extends { id: number }>({
  data,
  columns,
  pagination,
  onEdit,
  onDelete,
  onRowClick,
  isLoading = false,
  emptyMessage = 'データがありません',
}: DataTableProps<T>) {
  // 実装...
}
```

#### 使用例
```typescript
import { DataTable } from '@/components/common';
import { useFoods } from '@/api';
import type { ColumnDef } from '@/types/crud-config';
import type { Food } from '@/types';

function FoodListPage() {
  const { data, isLoading } = useFoods({ page: 1 });
  const navigate = useNavigate();

  // カラム定義
  const columns: ColumnDef<Food>[] = [
    {
      key: 'image',
      header: '画像',
      render: (food) => (
        <img src={food.image} alt={food.name} className="w-12 h-12 rounded" />
      ),
    },
    {
      key: 'name',
      header: '商品名',
      sortable: true,
    },
    {
      key: 'price',
      header: '価格',
      sortable: true,
      render: (food) => `¥${food.price.toLocaleString()}`,
    },
    {
      key: 'category',
      header: 'カテゴリ',
      render: (food) => <Badge>{food.category.name}</Badge>,
    },
    {
      key: 'status',
      header: 'ステータス',
      render: (food) => (
        <Badge variant={food.status ? 'success' : 'secondary'}>
          {food.status ? '公開中' : '非公開'}
        </Badge>
      ),
    },
  ];

  return (
    <DataTable
      data={data?.foods || []}
      columns={columns}
      pagination={{
        currentPage: data?.pagination.currentPage || 1,
        totalPages: data?.pagination.totalPages || 1,
        totalItems: data?.pagination.totalItems || 0,
        onPageChange: (page) => navigate(`?page=${page}`),
      }}
      onEdit={(food) => navigate(`/foods/${food.id}/edit`)}
      onDelete={(food) => handleDelete(food.id)}
      onRowClick={(food) => navigate(`/foods/${food.id}`)}
      isLoading={isLoading}
    />
  );
}
```

### 2.2 DynamicForm (汎用フォーム)

#### 概要
設定駆動型のフォームコンポーネント。バリデーション、エラー表示、ファイルアップロードを標準装備。

#### Props定義
```typescript
/**
 * DynamicForm.tsx
 * 
 * 汎用フォームコンポーネント
 * - React Hook Form統合
 * - 設定ベースのフィールド定義
 * - バリデーション対応
 * - ファイルアップロード対応
 */

import type { FormConfig } from '@/types/crud-config';

interface DynamicFormProps<T> {
  config: FormConfig<T>;                  // フォーム設定
  defaultValues?: Partial<T>;             // デフォルト値
  onSubmit: (data: T) => void | Promise<void>;
  isLoading?: boolean;
  submitButtonText?: string;
}

export function DynamicForm<T extends Record<string, any>>({
  config,
  defaultValues,
  onSubmit,
  isLoading = false,
  submitButtonText = '保存',
}: DynamicFormProps<T>) {
  const form = useForm<T>({
    defaultValues: defaultValues as any,
  });

  return (
    <form onSubmit={form.handleSubmit(onSubmit)}>
      {config.fields.map((field) => (
        <FormField
          key={field.name}
          field={field}
          form={form}
        />
      ))}
      <Button type="submit" disabled={isLoading}>
        {isLoading ? '送信中...' : submitButtonText}
      </Button>
    </form>
  );
}
```

#### 使用例
```typescript
import { DynamicForm } from '@/components/common';
import { useCreateFood } from '@/api';
import type { FormConfig } from '@/types/crud-config';
import type { CreateFoodRequest } from '@/types';

function FoodFormPage() {
  const createMutation = useCreateFood();
  const { data: categories } = useCategories();

  // フォーム設定
  const formConfig: FormConfig<CreateFoodRequest> = {
    fields: [
      {
        name: 'name',
        label: '商品名',
        type: 'text',
        required: true,
        validation: {
          minLength: { value: 3, message: '3文字以上入力してください' },
        },
      },
      {
        name: 'description',
        label: '説明',
        type: 'textarea',
        required: true,
      },
      {
        name: 'price',
        label: '価格',
        type: 'number',
        required: true,
        validation: {
          min: { value: 1, message: '1円以上入力してください' },
        },
      },
      {
        name: 'category_id',
        label: 'カテゴリ',
        type: 'select',
        required: true,
        options: categories?.map(cat => ({
          value: cat.id,
          label: cat.name,
        })) || [],
      },
      {
        name: 'image',
        label: '商品画像',
        type: 'file',
        required: true,
        accept: 'image/*',
      },
      {
        name: 'status',
        label: '公開',
        type: 'checkbox',
        defaultValue: true,
      },
    ],
  };

  const handleSubmit = async (data: CreateFoodRequest) => {
    const formData = new FormData();
    Object.entries(data).forEach(([key, value]) => {
      if (value instanceof FileList) {
        formData.append(key, value[0]);
      } else {
        formData.append(key, String(value));
      }
    });
    
    await createMutation.mutateAsync(formData);
  };

  return (
    <div>
      <h1>商品作成</h1>
      <DynamicForm
        config={formConfig}
        onSubmit={handleSubmit}
        isLoading={createMutation.isPending}
        submitButtonText="商品を作成"
      />
    </div>
  );
}
```

### 2.3 ProtectedRoute (認証・認可ガード)

#### 概要
ルート保護コンポーネント。未認証ユーザーのリダイレクトと権限チェック。

#### Props定義
```typescript
/**
 * ProtectedRoute.tsx
 * 
 * 認証・認可ガードコンポーネント
 * - 未認証ユーザーをログインページへリダイレクト
 * - 権限不足ユーザーをトップページへリダイレクト
 */

import { Navigate } from 'react-router-dom';
import { useRequireAuth } from '@/hooks';

interface ProtectedRouteProps {
  children: React.ReactNode;
  requiredRoles?: ('ADMIN' | 'USER')[];  // 必要な権限
}

export default function ProtectedRoute({
  children,
  requiredRoles,
}: ProtectedRouteProps) {
  const { isAuthenticated, user } = useRequireAuth();

  // 未認証チェック
  if (!isAuthenticated) {
    return <Navigate to="/login" replace />;
  }

  // 権限チェック
  if (requiredRoles && !requiredRoles.includes(user.role)) {
    return <Navigate to="/" replace />;
  }

  return <>{children}</>;
}
```

#### 使用例
```typescript
import { ProtectedRoute } from '@/components/common';
import { OrderListPage } from '@/pages/orders';

// App.tsx内のルーティング
<Route
  path="/orders"
  element={
    <ProtectedRoute requiredRoles={['ADMIN']}>
      <OrderListPage />
    </ProtectedRoute>
  }
/>
```

### 2.4 ActionMenu (編集・削除アクションメニュー)

#### 概要
データテーブル行のアクションメニュー（編集・削除）。

#### Props定義
```typescript
/**
 * ActionMenu.tsx
 * 
 * アクションメニューコンポーネント
 * - 編集・削除ボタン
 * - 確認ダイアログ表示
 */

interface ActionMenuProps {
  onEdit?: () => void;
  onDelete?: () => void;
  deleteConfirmMessage?: string;
}

export function ActionMenu({
  onEdit,
  onDelete,
  deleteConfirmMessage = '本当に削除しますか？',
}: ActionMenuProps) {
  const [showDeleteDialog, setShowDeleteDialog] = useState(false);

  return (
    <>
      <DropdownMenu>
        <DropdownMenuTrigger asChild>
          <Button variant="ghost" size="sm">
            <MoreVertical className="h-4 w-4" />
          </Button>
        </DropdownMenuTrigger>
        <DropdownMenuContent align="end">
          {onEdit && (
            <DropdownMenuItem onClick={onEdit}>
              <Edit className="mr-2 h-4 w-4" />
              編集
            </DropdownMenuItem>
          )}
          {onDelete && (
            <DropdownMenuItem
              onClick={() => setShowDeleteDialog(true)}
              className="text-red-600"
            >
              <Trash className="mr-2 h-4 w-4" />
              削除
            </DropdownMenuItem>
          )}
        </DropdownMenuContent>
      </DropdownMenu>

      <AlertDialog open={showDeleteDialog} onOpenChange={setShowDeleteDialog}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>削除確認</AlertDialogTitle>
            <AlertDialogDescription>
              {deleteConfirmMessage}
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>キャンセル</AlertDialogCancel>
            <AlertDialogAction onClick={onDelete}>削除</AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  );
}
```

---

## 3. ダッシュボードコンポーネント

### 3.1 StatsCard (統計カード)

#### 概要
数値統計を表示するカードコンポーネント。アイコン、トレンド、前期比較対応。

#### Props定義
```typescript
/**
 * StatsCard.tsx
 * 
 * 統計カード表示コンポーネント
 * - 数値・タイトル表示
 * - トレンド表示（増減率）
 * - アイコン表示
 */

interface StatsCardProps {
  title: string;                    // カードタイトル
  value: string | number;           // 表示値
  icon?: React.ReactNode;           // アイコン
  trend?: {
    value: number;                  // 増減率（%）
    isPositive: boolean;            // 増加（緑）or 減少（赤）
  };
  description?: string;             // 補足説明
}

export default function StatsCard({
  title,
  value,
  icon,
  trend,
  description,
}: StatsCardProps) {
  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between pb-2">
        <CardTitle className="text-sm font-medium">{title}</CardTitle>
        {icon}
      </CardHeader>
      <CardContent>
        <div className="text-2xl font-bold">{value}</div>
        {trend && (
          <p className={cn(
            'text-xs',
            trend.isPositive ? 'text-green-600' : 'text-red-600'
          )}>
            {trend.isPositive ? '↑' : '↓'} {Math.abs(trend.value)}%
          </p>
        )}
        {description && (
          <p className="text-xs text-muted-foreground">{description}</p>
        )}
      </CardContent>
    </Card>
  );
}
```

#### 使用例
```typescript
import { StatsCard } from '@/components/dashboard';
import { useDashboardOverview } from '@/api';
import { DollarSign, ShoppingCart, Package, Users } from 'lucide-react';

function DashboardPage() {
  const { data } = useDashboardOverview();

  return (
    <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
      <StatsCard
        title="総売上"
        value={data?.totalRevenue || '¥0'}
        icon={<DollarSign className="h-4 w-4 text-muted-foreground" />}
        trend={{ value: 12.5, isPositive: true }}
        description="前月比"
      />
      <StatsCard
        title="注文数"
        value={data?.totalOrders || 0}
        icon={<ShoppingCart className="h-4 w-4 text-muted-foreground" />}
        trend={{ value: 5.2, isPositive: true }}
      />
      <StatsCard
        title="商品数"
        value={data?.totalProducts || 0}
        icon={<Package className="h-4 w-4 text-muted-foreground" />}
      />
      <StatsCard
        title="ユーザー数"
        value={data?.totalUsers || 0}
        icon={<Users className="h-4 w-4 text-muted-foreground" />}
        trend={{ value: 2.1, isPositive: false }}
      />
    </div>
  );
}
```

### 3.2 SalesChart (売上推移チャート)

#### 概要
Rechartsを使用した売上推移の折れ線グラフ。

#### Props定義
```typescript
/**
 * SalesChart.tsx
 * 
 * 売上推移チャート（折れ線グラフ）
 * - Recharts使用
 * - 月別・日別表示切替対応
 */

import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts';

interface SalesChartProps {
  data: {
    date: string;     // 日付（YYYY-MM-DD）
    sales: number;    // 売上額
    orders: number;   // 注文数
  }[];
  title?: string;
}

export default function SalesChart({ data, title = '売上推移' }: SalesChartProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>{title}</CardTitle>
      </CardHeader>
      <CardContent>
        <ResponsiveContainer width="100%" height={300}>
          <LineChart data={data}>
            <CartesianGrid strokeDasharray="3 3" />
            <XAxis dataKey="date" />
            <YAxis />
            <Tooltip formatter={(value) => `¥${value.toLocaleString()}`} />
            <Legend />
            <Line
              type="monotone"
              dataKey="sales"
              stroke="#8884d8"
              name="売上"
              strokeWidth={2}
            />
            <Line
              type="monotone"
              dataKey="orders"
              stroke="#82ca9d"
              name="注文数"
              strokeWidth={2}
            />
          </LineChart>
        </ResponsiveContainer>
      </CardContent>
    </Card>
  );
}
```

### 3.3 OrderStatusChart (注文ステータス円グラフ)

#### 概要
Rechartsを使用した注文ステータス分布の円グラフ。

```typescript
/**
 * OrderStatusChart.tsx
 * 
 * 注文ステータス分布（円グラフ）
 */

import { PieChart, Pie, Cell, Tooltip, Legend, ResponsiveContainer } from 'recharts';

interface OrderStatusChartProps {
  data: {
    status: string;
    count: number;
    percentage: number;
  }[];
}

const COLORS = {
  PENDING: '#fbbf24',      // 黄色（処理待ち）
  PROCESSING: '#3b82f6',   // 青色（処理中）
  COMPLETED: '#10b981',    // 緑色（完了）
  CANCELLED: '#ef4444',    // 赤色（キャンセル）
};

export default function OrderStatusChart({ data }: OrderStatusChartProps) {
  const chartData = data.map(item => ({
    name: item.status,
    value: item.count,
    percentage: item.percentage,
  }));

  return (
    <Card>
      <CardHeader>
        <CardTitle>注文ステータス分布</CardTitle>
      </CardHeader>
      <CardContent>
        <ResponsiveContainer width="100%" height={300}>
          <PieChart>
            <Pie
              data={chartData}
              dataKey="value"
              nameKey="name"
              cx="50%"
              cy="50%"
              outerRadius={80}
              label={(entry) => `${entry.percentage.toFixed(1)}%`}
            >
              {chartData.map((entry, index) => (
                <Cell key={`cell-${index}`} fill={COLORS[entry.name]} />
              ))}
            </Pie>
            <Tooltip />
            <Legend />
          </PieChart>
        </ResponsiveContainer>
      </CardContent>
    </Card>
  );
}
```

### 3.4 TopProductsList (人気商品ランキング)

```typescript
/**
 * TopProductsList.tsx
 * 
 * 人気商品ランキング表示
 */

interface TopProductsListProps {
  products: {
    id: number;
    name: string;
    image: string;
    totalQuantity: number;
    totalRevenue: number;
  }[];
}

export default function TopProductsList({ products }: TopProductsListProps) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>人気商品トップ5</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="space-y-4">
          {products.map((product, index) => (
            <div key={product.id} className="flex items-center gap-4">
              <div className="flex-shrink-0 w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center font-bold">
                {index + 1}
              </div>
              <img
                src={product.image}
                alt={product.name}
                className="w-12 h-12 rounded object-cover"
              />
              <div className="flex-1 min-w-0">
                <p className="font-medium truncate">{product.name}</p>
                <p className="text-sm text-muted-foreground">
                  {product.totalQuantity}個販売
                </p>
              </div>
              <div className="text-right">
                <p className="font-semibold">
                  ¥{product.totalRevenue.toLocaleString()}
                </p>
              </div>
            </div>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
```

---

## 4. レイアウトコンポーネント

### 4.1 Layout (メインレイアウト)

```typescript
/**
 * Layout.tsx
 * 
 * メインレイアウトコンポーネント
 * - Header + Sidebar + Content の3カラムレイアウト
 * - レスポンシブ対応
 */

import { useState } from 'react';
import { Outlet } from 'react-router-dom';
import { Header } from './Header';
import { Sidebar } from './Sidebar';

export function Layout() {
  const [isSidebarOpen, setIsSidebarOpen] = useState(true);

  return (
    <div className="min-h-screen bg-gray-50">
      <Header onToggleSidebar={() => setIsSidebarOpen(!isSidebarOpen)} />
      
      <div className="flex">
        <Sidebar isOpen={isSidebarOpen} />
        
        <main className={cn(
          'flex-1 p-6 transition-all duration-300',
          isSidebarOpen ? 'ml-64' : 'ml-0'
        )}>
          <Outlet />
        </main>
      </div>
    </div>
  );
}
```

### 4.2 Header (ヘッダー)

```typescript
/**
 * Header.tsx
 * 
 * ヘッダーコンポーネント
 * - グローバル検索
 * - 通知アイコン
 * - ユーザーメニュー
 */

import { Menu, Bell, User, LogOut } from 'lucide-react';
import { GlobalSearch } from '@/components/common';
import { useAuth, useLogout } from '@/api';

interface HeaderProps {
  onToggleSidebar: () => void;
}

export function Header({ onToggleSidebar }: HeaderProps) {
  const { user } = useAuth();
  const logoutMutation = useLogout();

  return (
    <header className="h-16 bg-white border-b border-gray-200 px-6 flex items-center justify-between">
      {/* 左側: メニューボタン + 検索 */}
      <div className="flex items-center gap-4">
        <Button variant="ghost" size="icon" onClick={onToggleSidebar}>
          <Menu className="h-5 w-5" />
        </Button>
        <GlobalSearch />
      </div>

      {/* 右側: 通知 + ユーザーメニュー */}
      <div className="flex items-center gap-4">
        <Button variant="ghost" size="icon">
          <Bell className="h-5 w-5" />
        </Button>
        
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button variant="ghost" className="flex items-center gap-2">
              <Avatar>
                <AvatarFallback>{user?.name.charAt(0)}</AvatarFallback>
              </Avatar>
              <span>{user?.name}</span>
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            <DropdownMenuItem>
              <User className="mr-2 h-4 w-4" />
              プロフィール
            </DropdownMenuItem>
            <DropdownMenuItem onClick={() => logoutMutation.mutate()}>
              <LogOut className="mr-2 h-4 w-4" />
              ログアウト
            </DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </header>
  );
}
```

### 4.3 Sidebar (サイドバー)

```typescript
/**
 * Sidebar.tsx
 * 
 * サイドバーナビゲーション
 * - 権限に応じたメニュー表示
 * - アクティブ状態ハイライト
 */

import { Link, useLocation } from 'react-router-dom';
import { Home, Package, ShoppingCart, FolderOpen } from 'lucide-react';
import { useAuth } from '@/hooks';

interface SidebarProps {
  isOpen: boolean;
}

export function Sidebar({ isOpen }: SidebarProps) {
  const location = useLocation();
  const { user } = useAuth();

  const menuItems = [
    { path: '/', label: 'ダッシュボード', icon: Home, roles: ['ADMIN', 'USER'] },
    { path: '/foods', label: '商品管理', icon: Package, roles: ['ADMIN', 'USER'] },
    { path: '/orders', label: '注文管理', icon: ShoppingCart, roles: ['ADMIN'] },
    { path: '/categories', label: 'カテゴリ管理', icon: FolderOpen, roles: ['ADMIN'] },
  ];

  // 権限フィルタリング
  const visibleItems = menuItems.filter(item => 
    item.roles.includes(user?.role || 'USER')
  );

  return (
    <aside className={cn(
      'fixed top-16 left-0 h-[calc(100vh-4rem)] w-64 bg-white border-r border-gray-200 transition-transform duration-300',
      isOpen ? 'translate-x-0' : '-translate-x-full'
    )}>
      <nav className="p-4 space-y-2">
        {visibleItems.map(item => {
          const Icon = item.icon;
          const isActive = location.pathname === item.path;
          
          return (
            <Link
              key={item.path}
              to={item.path}
              className={cn(
                'flex items-center gap-3 px-4 py-2 rounded-lg transition-colors',
                isActive
                  ? 'bg-primary text-primary-foreground'
                  : 'hover:bg-gray-100'
              )}
            >
              <Icon className="h-5 w-5" />
              <span>{item.label}</span>
            </Link>
          );
        })}
      </nav>
    </aside>
  );
}
```

---

## 5. UIコンポーネント

### 5.1 shadcn/ui コンポーネント一覧

プロジェクトでは以下のshadcn/uiコンポーネントを使用：

| コンポーネント | 用途 |
|---------------|------|
| `Button` | ボタン（primary, secondary, ghost, link） |
| `Card` | カード表示（CardHeader, CardTitle, CardContent） |
| `Input` | テキスト入力 |
| `Textarea` | 複数行テキスト入力 |
| `Select` | ドロップダウン選択 |
| `Checkbox` | チェックボックス |
| `Label` | フォームラベル |
| `Table` | テーブル表示 |
| `Badge` | バッジ（ステータス表示） |
| `DropdownMenu` | ドロップダウンメニュー |
| `Avatar` | ユーザーアバター |
| `Progress` | プログレスバー |

### 5.2 カスタムバリアント例

```typescript
// Button コンポーネントのカスタムバリアント
<Button variant="default">デフォルト</Button>
<Button variant="destructive">削除</Button>
<Button variant="outline">アウトライン</Button>
<Button variant="secondary">セカンダリ</Button>
<Button variant="ghost">ゴースト</Button>
<Button variant="link">リンク</Button>

// Badge コンポーネントのカスタムバリアント
<Badge variant="default">デフォルト</Badge>
<Badge variant="success">成功</Badge>
<Badge variant="warning">警告</Badge>
<Badge variant="destructive">エラー</Badge>
<Badge variant="secondary">セカンダリ</Badge>
```

---

## 6. コンポーネント合成パターン

### 6.1 Compound Components（複合コンポーネント）

```typescript
// ✅ 良い例: 複合コンポーネントパターン
<Card>
  <CardHeader>
    <CardTitle>タイトル</CardTitle>
    <CardDescription>説明</CardDescription>
  </CardHeader>
  <CardContent>
    コンテンツ
  </CardContent>
  <CardFooter>
    <Button>アクション</Button>
  </CardFooter>
</Card>

// ❌ 避けるべき: 全てをPropsで渡す
<Card
  title="タイトル"
  description="説明"
  content="コンテンツ"
  footer={<Button>アクション</Button>}
/>
```

### 6.2 Render Props パターン

```typescript
// DataTableでのRender Props使用例
<DataTable
  data={foods}
  columns={[
    {
      key: 'name',
      header: '商品名',
      render: (food) => (
        <div className="flex items-center gap-2">
          <img src={food.image} className="w-8 h-8 rounded" />
          <span className="font-medium">{food.name}</span>
        </div>
      ),
    },
  ]}
/>
```

### 6.3 Higher-Order Components (HOC)

```typescript
// withAuth HOC例
function withAuth<P extends object>(
  Component: React.ComponentType<P>,
  requiredRoles?: string[]
) {
  return function AuthenticatedComponent(props: P) {
    const { isAuthenticated, user } = useAuth();

    if (!isAuthenticated) {
      return <Navigate to="/login" />;
    }

    if (requiredRoles && !requiredRoles.includes(user.role)) {
      return <Navigate to="/" />;
    }

    return <Component {...props} />;
  };
}

// 使用例
const ProtectedOrderPage = withAuth(OrderListPage, ['ADMIN']);
```

---

## 7. スタイリング戦略

### 7.1 Tailwind CSS クラス命名規則

```typescript
// ✅ 推奨: ユーティリティファースト
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow">

// ✅ 推奨: cn() ヘルパーで条件付きクラス
import { cn } from '@/lib/utils';

<Button
  className={cn(
    'px-4 py-2',
    isActive && 'bg-primary text-white',
    isDisabled && 'opacity-50 cursor-not-allowed'
  )}
>

// ❌ 避けるべき: インラインスタイル
<div style={{ display: 'flex', padding: '16px' }}>
```

### 7.2 レスポンシブデザイン

```typescript
// Tailwindのブレークポイント使用
<div className="
  grid
  grid-cols-1           // モバイル: 1列
  md:grid-cols-2        // タブレット: 2列
  lg:grid-cols-4        // デスクトップ: 4列
  gap-4
">
```

### 7.3 ダークモード対応（将来実装）

```typescript
// Tailwind CSS のダークモード対応
<div className="
  bg-white dark:bg-gray-800
  text-gray-900 dark:text-gray-100
  border-gray-200 dark:border-gray-700
">
```

---

## 📚 参考資料

- [shadcn/ui公式ドキュメント](https://ui.shadcn.com/)
- [Tailwind CSS公式ドキュメント](https://tailwindcss.com/docs)
- [Recharts公式ドキュメント](https://recharts.org/)
- [React Design Patterns](https://www.patterns.dev/posts/reactpatterns)

---

**最終更新**: 2025-10-15  
**バージョン**: 1.0.0
