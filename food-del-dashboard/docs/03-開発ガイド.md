# 03-開発ガイド

> Food-Del-Dashboard プロジェクトの開発規約と実践ガイド

## 📋 目次

1. [コーディング規約](#コーディング規約)
2. [TypeScript使用ガイドライン](#typescript使用ガイドライン)
3. [新機能追加ワークフロー](#新機能追加ワークフロー)
4. [Barrel Exportパターン実践](#barrel-exportパターン実践)
5. [TanStack Query使用ガイド](#tanstack-query使用ガイド)
6. [状態管理ベストプラクティス](#状態管理ベストプラクティス)
7. [コンポーネント開発規約](#コンポーネント開発規約)
8. [デバッグとトラブルシューティング](#デバッグとトラブルシューティング)
9. [パフォーマンス最適化](#パフォーマンス最適化)
10. [Gitワークフロー](#gitワークフロー)

---

## 1. コーディング規約

### 1.1 命名規則

#### ファイル名
```
✅ 正しい命名:
- PascalCase: FoodListPage.tsx, StatsCard.tsx, ProtectedRoute.tsx
- camelCase: authApi.ts, httpClient.ts, useAuth.ts
- kebab-case: food-list.css (CSSファイルのみ)

❌ 避けるべき命名:
- food_list_page.tsx (snake_case)
- foodlistpage.tsx (全小文字)
- Food-List-Page.tsx (kebab-case for TSX)
```

#### 変数・関数命名
```typescript
// ✅ 正しい命名
const userName = 'John';                      // camelCase
const MAX_RETRY_COUNT = 3;                    // UPPER_SNAKE_CASE (定数)
function fetchUserData() {}                   // camelCase (動詞始まり)
const handleSubmit = () => {};                // camelCase (イベントハンドラ: handle*)
const isLoading = true;                       // boolean: is*, has*, should*
const hasPermission = false;
const shouldShowModal = true;

// ❌ 避けるべき命名
const UserName = 'John';                      // PascalCaseは型のみ
const user_name = 'John';                     // snake_case
function data() {}                            // 動詞がない
const loading = true;                         // booleanが不明確
```

#### 型・インターフェース命名
```typescript
// ✅ 正しい命名
interface User {}                             // PascalCase
interface LoginRequest {}                     // 具体的な名前
type ApiResponse<T> = { data: T };           // 型パラメータ: T, U, V...
enum OrderStatus {}                           // PascalCase

// ❌ 避けるべき命名
interface IUser {}                            // Iプレフィックス不要
interface user {}                             // 小文字
type Data = {};                               // 抽象的すぎる
```

### 1.2 コメント規約

#### ファイルヘッダーコメント
```typescript
/**
 * FoodListPage.tsx
 * 
 * 商品一覧ページコンポーネント
 * - 商品のページネーション表示
 * - カテゴリ・ステータス・検索フィルタリング
 * - 管理者のみ作成・編集・削除可能
 * 
 * @author Food-Del Team
 * @version 1.0.0
 */
```

#### 関数・メソッドコメント
```typescript
// ✅ 正しいコメント（JSDoc形式）
/**
 * ユーザーをログインさせ、トークンを保存する
 * @param email - ユーザーのメールアドレス
 * @param password - パスワード
 * @returns Promise<User> ログインしたユーザー情報
 * @throws ApiError 認証失敗時
 */
async function login(email: string, password: string): Promise<User> {
  // ...
}

// ✅ シンプルな関数は単一行コメント
// 現在ログイン中のユーザーを取得
function getCurrentUser() {
  return authStore.getState().user;
}
```

#### インラインコメント
```typescript
// ✅ 正しいコメント
// HACK: TanStack Query v5では onError が削除されたため、エラーハンドリングは手動で行う
const mutation = useMutation({ ... });

// TODO: 画像アップロード時のプログレスバー実装
// FIXME: ページネーション時のスクロール位置がリセットされる問題
// NOTE: このAPIは将来的に廃止予定（v2.0で削除）

// ❌ 避けるべきコメント
// ユーザーを取得
const user = getUser();  // 自明なコメント

// これはバグです
// とりあえず動きます  // 不適切な表現
```

### 1.3 コードスタイル

#### インポート順序
```typescript
// 1. React関連
import React, { useState, useEffect } from 'react';

// 2. 外部ライブラリ
import { useQuery } from '@tanstack/react-query';
import { useNavigate } from 'react-router-dom';

// 3. 内部モジュール（Barrel Export使用）
import { useFoods } from '@/api';
import { DataTable } from '@/components/common';
import { useAuth } from '@/hooks';
import type { Food } from '@/types';

// 4. 相対パス（同一ディレクトリ内のみ）
import { FoodCard } from './FoodCard';

// 5. スタイル
import './FoodListPage.css';
```

#### 関数定義スタイル
```typescript
// ✅ コンポーネント: function宣言（ホイスティング利用）
export default function FoodListPage() {
  return <div>...</div>;
}

// ✅ カスタムフック: function宣言
export function useAuth() {
  return { ... };
}

// ✅ イベントハンドラ: アロー関数
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  // ...
};

// ✅ ユーティリティ関数: function宣言
export function formatPrice(price: number): string {
  return `¥${price.toLocaleString()}`;
}
```

---

## 2. TypeScript使用ガイドライン

### 2.1 型定義の優先順位

```typescript
// ✅ 優先順位: interface > type
// オブジェクト構造はinterfaceを使用（拡張可能）
interface User {
  id: number;
  name: string;
}

interface Admin extends User {
  role: 'ADMIN';
}

// ✅ Union型、Mapped型はtypeを使用
type OrderStatus = 'PENDING' | 'PROCESSING' | 'COMPLETED' | 'CANCELLED';
type Nullable<T> = T | null;
type ReadonlyUser = Readonly<User>;
```

### 2.2 型アサーションの避け方

```typescript
// ❌ 避けるべき: 型アサーション乱用
const user = data as User;
const element = document.getElementById('root') as HTMLElement;

// ✅ 推奨: 型ガード使用
function isUser(data: unknown): data is User {
  return (
    typeof data === 'object' &&
    data !== null &&
    'id' in data &&
    'name' in data
  );
}

if (isUser(data)) {
  console.log(data.name);  // 型安全
}

// ✅ 推奨: Zodなどのバリデーションライブラリ（将来実装）
import { z } from 'zod';

const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
});

const user = UserSchema.parse(data);  // 実行時バリデーション
```

### 2.3 ジェネリクスの活用

```typescript
// ✅ API応答の型安全な定義
interface ApiResponse<T> {
  success: boolean;
  data: T;
  message?: string;
}

async function fetchData<T>(url: string): Promise<ApiResponse<T>> {
  const response = await httpClient.get<ApiResponse<T>>(url);
  return response.data;
}

// 使用例
const foodResponse = await fetchData<Food[]>('/api/foods/list');
// foodResponse.data は Food[] 型

// ✅ 再利用可能なカスタムフック
function useApiQuery<T>(
  queryKey: string[],
  queryFn: () => Promise<T>
) {
  return useQuery({
    queryKey,
    queryFn,
    staleTime: 5 * 60 * 1000,
  });
}
```

### 2.4 Utility Typesの活用

```typescript
// ✅ Partial: 一部のプロパティのみ必須
type UpdateFoodRequest = Partial<Food>;

// ✅ Pick: 特定プロパティのみ抽出
type FoodPreview = Pick<Food, 'id' | 'name' | 'image' | 'price'>;

// ✅ Omit: 特定プロパティを除外
type FoodWithoutId = Omit<Food, 'id'>;

// ✅ Required: 全プロパティ必須化
type CompletedOrder = Required<Order>;

// ✅ Record: キー・バリュー型定義
type FoodMap = Record<number, Food>;
```

---

## 3. 新機能追加ワークフロー

### 3.1 新しいページの追加

#### ステップ1: 型定義作成
```bash
# src/types/example.ts
```

```typescript
/**
 * example.ts
 * 
 * Example機能の型定義
 */

export interface Example {
  id: number;
  name: string;
  description: string;
  createdAt: string;
}

export interface CreateExampleRequest {
  name: string;
  description: string;
}

export interface UpdateExampleRequest extends Partial<CreateExampleRequest> {}
```

#### ステップ2: API関数作成
```bash
# src/api/exampleApi.ts
```

```typescript
/**
 * exampleApi.ts
 * 
 * Example機能のAPI通信関数
 */

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { httpClient } from '@/lib/httpClient';
import { API_ENDPOINTS } from '@/lib/apiConstants';
import type { Example, CreateExampleRequest } from '@/types';

// 一覧取得
export const useExamples = () => {
  return useQuery({
    queryKey: ['examples'],
    queryFn: async () => {
      const response = await httpClient.get<Example[]>(
        API_ENDPOINTS.EXAMPLES.LIST
      );
      return response.data;
    },
    staleTime: 5 * 60 * 1000,  // 5分間キャッシュ
  });
};

// 作成
export const useCreateExample = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async (data: CreateExampleRequest) => {
      const response = await httpClient.post(
        API_ENDPOINTS.EXAMPLES.CREATE,
        data
      );
      return response.data;
    },
    onSuccess: () => {
      // 一覧キャッシュを無効化
      queryClient.invalidateQueries({ queryKey: ['examples'] });
    },
  });
};
```

#### ステップ3: Barrel Exportに追加
```typescript
// src/api/index.ts に追加
export * from './exampleApi';

// src/types/index.ts に追加
export * from './example';
```

#### ステップ4: APIエンドポイント定義
```typescript
// src/lib/apiConstants.ts に追加
export const API_ENDPOINTS = {
  // ... 既存のエンドポイント
  EXAMPLES: {
    LIST: '/api/examples/list',
    DETAIL: (id: number) => `/api/examples/detail/${id}`,
    CREATE: '/api/examples/create',
    UPDATE: (id: number) => `/api/examples/update/${id}`,
    DELETE: (id: number) => `/api/examples/delete/${id}`,
  },
};
```

#### ステップ5: ページコンポーネント作成
```bash
# src/pages/examples/ExampleListPage.tsx
```

```typescript
/**
 * ExampleListPage.tsx
 * 
 * Example一覧ページ
 */

import { useExamples } from '@/api';
import { DataTable } from '@/components/common';

export default function ExampleListPage() {
  const { data: examples, isLoading } = useExamples();

  if (isLoading) return <div>Loading...</div>;

  return (
    <div>
      <h1>Examples</h1>
      <DataTable data={examples || []} columns={[...]} />
    </div>
  );
}
```

#### ステップ6: Barrel Export作成
```typescript
// src/pages/examples/index.ts
export { default as ExampleListPage } from './ExampleListPage';
export { default as ExampleDetailPage } from './ExampleDetailPage';
export { default as ExampleFormPage } from './ExampleFormPage';
```

#### ステップ7: ルーティング追加
```typescript
// src/App.tsx
import { ExampleListPage, ExampleDetailPage, ExampleFormPage } from '@/pages/examples';

// ProtectedRoutes内に追加
<Route path="examples">
  <Route index element={<ExampleListPage />} />
  <Route path=":id" element={<ExampleDetailPage />} />
  <Route path="new" element={<ProtectedRoute requiredRoles={['ADMIN']}><ExampleFormPage /></ProtectedRoute>} />
  <Route path=":id/edit" element={<ProtectedRoute requiredRoles={['ADMIN']}><ExampleFormPage /></ProtectedRoute>} />
</Route>
```

### 3.2 新しいコンポーネントの追加

#### ステップ1: コンポーネント作成
```bash
# src/components/common/ExampleCard.tsx
```

```typescript
/**
 * ExampleCard.tsx
 * 
 * Example表示用カードコンポーネント
 */

import type { Example } from '@/types';

interface ExampleCardProps {
  example: Example;
  onEdit?: (id: number) => void;
  onDelete?: (id: number) => void;
}

export function ExampleCard({ example, onEdit, onDelete }: ExampleCardProps) {
  return (
    <div className="border rounded-lg p-4">
      <h3>{example.name}</h3>
      <p>{example.description}</p>
      {onEdit && <button onClick={() => onEdit(example.id)}>Edit</button>}
      {onDelete && <button onClick={() => onDelete(example.id)}>Delete</button>}
    </div>
  );
}
```

#### ステップ2: Barrel Exportに追加
```typescript
// src/components/common/index.ts に追加
export * from './ExampleCard';
```

---

## 4. Barrel Exportパターン実践

### 4.1 Barrel Exportの原則

```
原則1: 各ディレクトリに index.ts を配置
原則2: 外部からはディレクトリ名でインポート
原則3: ディレクトリ内部では相対パスOK
```

### 4.2 Named Export vs Default Export

```typescript
// ✅ Named Export推奨（複数エクスポート可能）
// src/components/common/DataTable.tsx
export function DataTable<T>({ data }: { data: T[] }) {
  return <table>...</table>;
}

export function DataTableHeader() {
  return <thead>...</thead>;
}

// src/components/common/index.ts
export * from './DataTable';  // シンプル

// 使用側
import { DataTable, DataTableHeader } from '@/components/common';
```

```typescript
// ✅ Default Export（1ファイル1コンポーネントの場合）
// src/components/layout/Header.tsx
export default function Header() {
  return <header>...</header>;
}

// src/components/layout/index.ts
export { default as Header } from './Header';

// 使用側
import { Header } from '@/components/layout';
```

### 4.3 よくある間違いと修正

```typescript
// ❌ 間違い: 直接ファイルをインポート
import { DataTable } from '@/components/common/DataTable';
import StatsCard from '@/components/dashboard/StatsCard';

// ✅ 正しい: Barrel Exportを使用
import { DataTable } from '@/components/common';
import { StatsCard } from '@/components/dashboard';
```

---

## 5. TanStack Query使用ガイド

### 5.1 Query Keyの命名規則

```typescript
// ✅ 推奨: 階層的なキー構造
const queryKey = ['foods'];                          // 一覧
const queryKey = ['foods', id];                      // 詳細
const queryKey = ['foods', 'list', { page, search }]; // フィルタ付き一覧
const queryKey = ['foods', id, 'reviews'];           // 関連データ

// ❌ 避けるべき: フラットなキー
const queryKey = ['foodList'];
const queryKey = ['food123'];
```

### 5.2 キャッシュ戦略

```typescript
// ✅ データ特性に応じたstaleTime設定
export const useOrders = () => {
  return useQuery({
    queryKey: ['orders'],
    queryFn: fetchOrders,
    staleTime: 1 * 60 * 1000,  // 1分（頻繁に更新されるデータ）
  });
};

export const useFoods = () => {
  return useQuery({
    queryKey: ['foods'],
    queryFn: fetchFoods,
    staleTime: 5 * 60 * 1000,  // 5分（安定したデータ）
  });
};

export const useCategories = () => {
  return useQuery({
    queryKey: ['categories'],
    queryFn: fetchCategories,
    staleTime: 10 * 60 * 1000,  // 10分（ほぼ静的なデータ）
  });
};
```

### 5.3 楽観的更新パターン

```typescript
export const useUpdateFood = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: async ({ id, data }: { id: number; data: UpdateFoodRequest }) => {
      const response = await httpClient.patch(
        API_ENDPOINTS.FOODS.UPDATE(id),
        data
      );
      return response.data;
    },
    // 楽観的更新: リクエスト前にUIを即座に更新
    onMutate: async ({ id, data }) => {
      // 進行中のクエリをキャンセル
      await queryClient.cancelQueries({ queryKey: ['foods'] });
      
      // 現在のデータを保存（ロールバック用）
      const previousFoods = queryClient.getQueryData(['foods']);
      
      // 楽観的にキャッシュを更新
      queryClient.setQueryData(['foods'], (old: Food[] = []) => {
        return old.map(food => 
          food.id === id ? { ...food, ...data } : food
        );
      });
      
      // ロールバック用データを返す
      return { previousFoods };
    },
    // エラー時: ロールバック
    onError: (err, variables, context) => {
      if (context?.previousFoods) {
        queryClient.setQueryData(['foods'], context.previousFoods);
      }
    },
    // 成功時: サーバーデータでキャッシュ更新
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['foods'] });
    },
  });
};
```

---

## 6. 状態管理ベストプラクティス

### 6.1 Zustand vs TanStack Query の使い分け

```typescript
// ✅ Zustand: グローバルUI状態（認証、テーマなど）
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface AuthState {
  user: User | null;
  accessToken: string | null;
  login: (user: User, token: string) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      accessToken: null,
      login: (user, accessToken) => set({ user, accessToken }),
      logout: () => set({ user: null, accessToken: null }),
    }),
    { name: 'auth-storage' }
  )
);

// ✅ TanStack Query: サーバー状態（商品データ、注文データなど）
export const useFoods = () => {
  return useQuery({
    queryKey: ['foods'],
    queryFn: fetchFoods,
  });
};
```

### 6.2 状態の派生（Derived State）

（省略）

### 6.3 複数の非同期データソースからの状態派生

ダッシュボードのように、複数の非同期データソース（複数の `useQuery` フック）から得たデータを組み合わせて表示するコンポーネントでは、データソースの管理が複雑になりがちです。

**悪い例: JSX内でのデータソース混在**

JSX内で直接、複数のデータソースを `||` や三項演算子で結合すると、ロジックが分散し、可読性とメンテナンス性が著しく低下します。

```tsx
// DashboardPage.tsx (リファクタリング前)

const overviewQuery = useDashboardOverview();
const orderStatsQuery = useOrderStats();

return (
    <StatsCard
        // 👇 データソースがJSX内に混在している
        value={orderStatsQuery.data?.total_revenue || overviewQuery.data?.totalRevenue.value}
        loading={orderStatsQuery.isLoading} // ローディング状態も不整合
    />
);
```

**良い例: 「ビューモデル」パターンでデータソースを統一**

コンポーネントのロジック層（レンダリング前）で `useMemo` を使い、すべてのデータソースを一つのクリーンな「ビューモデル」オブジェクトに集約します。UIコンポーネントは、この統一されたオブジェクトからのみデータを読み取ります。

```tsx
// DashboardPage.tsx (リファクタリング後)

// 1. データ取得
const overviewQuery = useDashboardOverview();
const orderStatsQuery = useOrderStats();

// 2. useMemoを使って、単一のビューモデルを構築
const unifiedStats = useMemo(() => ({
    totalRevenue: orderStatsQuery.data?.total_revenue ?? overviewQuery.data?.totalRevenue.value,
    totalOrders: orderStatsQuery.data?.total_orders ?? overviewQuery.data?.totalOrders.value,
    // ... 他のデータも同様に統一
}), [orderStatsQuery.data, overviewQuery.data]);

// 3. UIは統一されたビューモデルからのみデータを読み取る
return (
    <StatsCard
        title="総売上"
        value={unifiedStats.totalRevenue} // ✨ クリーンなデータソース
        loading={orderStatsQuery.isLoading || overviewQuery.isLoading} // ローディング状態もここで統一
    />
);
```

このパターンにより、「データ加工ロジック」と「UIレンダリングロジック」が明確に分離され、コンポーネント全体の見通しが格段に良くなります。

---

## 7. コンポーネント開発規約

### 7.1 コンポーネント構造

```typescript
/**
 * FoodCard.tsx
 * 
 * 商品カード表示コンポーネント
 */

// 1. Import文
import React from 'react';
import { Card, CardHeader, CardContent } from '@/components/ui/card';
import type { Food } from '@/types';

// 2. Props型定義
interface FoodCardProps {
  food: Food;
  onEdit?: (id: number) => void;
  onDelete?: (id: number) => void;
  className?: string;
}

// 3. サブコンポーネント（必要に応じて）
function FoodCardImage({ src, alt }: { src: string; alt: string }) {
  return <img src={src} alt={alt} className="w-full h-48 object-cover" />;
}

// 4. メインコンポーネント
export function FoodCard({ food, onEdit, onDelete, className }: FoodCardProps) {
  // 4.1 フック呼び出し
  const [isDeleting, setIsDeleting] = React.useState(false);
  
  // 4.2 イベントハンドラ
  const handleDelete = async () => {
    setIsDeleting(true);
    try {
      await onDelete?.(food.id);
    } finally {
      setIsDeleting(false);
    }
  };
  
  // 4.3 レンダリング
  return (
    <Card className={className}>
      <CardHeader>
        <FoodCardImage src={food.image} alt={food.name} />
      </CardHeader>
      <CardContent>
        <h3>{food.name}</h3>
        <p>¥{food.price.toLocaleString()}</p>
        {onEdit && <button onClick={() => onEdit(food.id)}>編集</button>}
        {onDelete && <button onClick={handleDelete} disabled={isDeleting}>削除</button>}
      </CardContent>
    </Card>
  );
}
```

### 7.2 Props設計原則

（省略）

### 7.3 データ編集フォームの実装パターン

（省略）

### 7.4 再利用可能なフォームコンポーネントの実装

`<DynamicForm>` のような、設定駆動で再利用可能なフォームコンポーネントを実装する際は、親コンポーネントの非同期的な性質を考慮する必要があります。

#### 1. 非同期な初期値の処理 (`useEffect` + `reset`)

編集フォームでは、親コンポーネントがAPIからデータを取得するまで、フォームの初期値 (`initialValues`) は `undefined` または空です。`react-hook-form` の `useForm` は、`defaultValues` を初期マウント時に一度しか使用しないため、データ取得後にフォームを更新する仕組みが必須です。

**実装パターン:**
`useEffect` を使用して `initialValues` の変更を監視し、変更があった場合に `reset` 関数を呼び出してフォーム全体を更新します。

```typescript
// src/components/common/DynamicForm.tsx

import React, { useEffect } from 'react';
import { useForm } from 'react-hook-form';

export const DynamicForm: React.FC<DynamicFormProps> = ({
    initialValues = {},
    // ... other props
}) => {
    const { reset, ...rest } = useForm({
        defaultValues: initialValues,
    });

    // ✅ initialValues が親コンポーネントのデータ取得によって変更されたら、
    //    フォームの値をリセットしてUIに反映させる
    useEffect(() => {
        if (initialValues) {
            reset(initialValues);
        }
    }, [initialValues, reset]);

    return <form>...</form>;
};
```

#### 2. 送信状態の管理 (`isSubmitting` Prop)

フォームの送信処理（API呼び出し）は親コンポーネントが担当します。そのため、フォームコンポーネント自身は「現在送信中かどうか」を知ることができません。送信中の状態をUIに反映させる（例：ボタンを無効化する）には、親から `isSubmitting` のような状態をpropとして受け取る必要があります。

**実装パターン:**
`isSubmitting` propを定義し、それを使って送信ボタンやフォームフィールドの `disabled` 状態を制御します。

```typescript
// src/components/common/DynamicForm.tsx

interface DynamicFormProps {
    // ... other props
    isSubmitting?: boolean;
}

export const DynamicForm: React.FC<DynamicFormProps> = ({
    isSubmitting = false,
    // ... other props
}) => {
    // ...

    return (
        <form>
            {/* 各フォームフィールド */}
            <FormField disabled={isSubmitting || field.disabled} />

            {/* 送信ボタン */}
            <Button type="submit" disabled={isSubmitting}>
                {isSubmitting ? '保存中...' : '保存'}
            </Button>
        </form>
    );
};
```

この2つのパターンを実装することで、`DynamicForm` は親コンポーネントの非同期的なライフサイクルと完全に協調して動作する、堅牢なコンポーネントになります。

---

## 8. デバッグとトラブルシューティング

### 8.1 TanStack Query Devtools

```typescript
// src/main.tsx に追加
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';

root.render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <App />
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  </React.StrictMode>
);
```

### 8.2 よくあるエラーと解決法

#### エラー1: "No QueryClient set"
```typescript
// ❌ 原因: QueryClientProviderの外でuseQueryを使用
function App() {
  const { data } = useQuery(...);  // エラー
  return <div>{data}</div>;
}

// ✅ 解決: QueryClientProvider内で使用
function App() {
  return (
    <QueryClientProvider client={queryClient}>
      <FoodList />
    </QueryClientProvider>
  );
}

function FoodList() {
  const { data } = useQuery(...);  // OK
  return <div>{data}</div>;
}
```

#### エラー2: "Cannot read property 'name' of undefined"
```typescript
// ❌ 原因: データ読み込み中にアクセス
function FoodDetail() {
  const { data } = useFood(id);
  return <h1>{data.name}</h1>;  // data が undefined の可能性
}

// ✅ 解決: ローディング状態をチェック
function FoodDetail() {
  const { data, isLoading } = useFood(id);
  
  if (isLoading) return <div>Loading...</div>;
  if (!data) return <div>Not found</div>;
  
  return <h1>{data.name}</h1>;  // 安全
}
```

#### エラー3: "401 Unauthorized"
```typescript
// 原因: トークン期限切れ、または未認証
// 解決: httpClient.ts の refreshToken ロジックを確認

// デバッグ方法
console.log('Access Token:', authStore.getState().accessToken);
console.log('Refresh Token:', authStore.getState().refreshToken);

// トークンをデコードして期限を確認
function decodeJWT(token: string) {
  const base64Url = token.split('.')[1];
  const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');
  const jsonPayload = decodeURIComponent(
    atob(base64)
      .split('')
      .map(c => '%' + ('00' + c.charCodeAt(0).toString(16)).slice(-2))
      .join('')
  );
  return JSON.parse(jsonPayload);
}

const payload = decodeJWT(accessToken);
console.log('Token expires at:', new Date(payload.exp * 1000));
```

### 8.3 パフォーマンスデバッグ

```typescript
// React Profilerを使用
import { Profiler } from 'react';

function App() {
  const onRenderCallback = (
    id: string,
    phase: 'mount' | 'update',
    actualDuration: number
  ) => {
    console.log(`${id} (${phase}): ${actualDuration}ms`);
  };

  return (
    <Profiler id="App" onRender={onRenderCallback}>
      <YourComponent />
    </Profiler>
  );
}

// useMemoとuseCallbackのデバッグ
import { useCallback, useMemo } from 'react';

function ExpensiveComponent({ data }: { data: Data[] }) {
  // ✅ 高コスト計算はuseMemoでメモ化
  const processedData = useMemo(() => {
    console.log('Processing data...');  // デバッグ: 何回呼ばれるか確認
    return data.map(item => expensiveOperation(item));
  }, [data]);

  // ✅ コールバックはuseCallbackでメモ化
  const handleClick = useCallback(() => {
    console.log('Clicked');
  }, []);

  return <div>...</div>;
}
```

---

## 9. パフォーマンス最適化

### 9.1 コード分割（React.lazy）

```typescript
// src/App.tsx
import { lazy, Suspense } from 'react';

// ✅ 動的インポート: 初期バンドルサイズを削減
const DashboardPage = lazy(() => import('@/pages/dashboard').then(m => ({ default: m.DashboardPage })));
const OrderListPage = lazy(() => import('@/pages/orders').then(m => ({ default: m.OrderListPage })));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <Routes>
        <Route path="/" element={<DashboardPage />} />
        <Route path="/orders" element={<OrderListPage />} />
      </Routes>
    </Suspense>
  );
}
```

### 9.2 画像最適化

```typescript
// ✅ WebP形式 + 遅延読み込み
function FoodImage({ src, alt }: { src: string; alt: string }) {
  return (
    <picture>
      <source srcSet={src.replace('.png', '.webp')} type="image/webp" />
      <img 
        src={src} 
        alt={alt} 
        loading="lazy"  // ブラウザネイティブの遅延読み込み
        decoding="async"
      />
    </picture>
  );
}

// ✅ サムネイル版を使用
function FoodCard({ food }: { food: Food }) {
  const thumbnailUrl = food.image.replace('/uploads/', '/uploads/thumbnails/');
  return <img src={thumbnailUrl} alt={food.name} />;
}
```

### 9.3 リスト仮想化

（省略）

### 9.4 コンポーネントの再レンダリング最適化

Reactコンポーネントは、自身のstateまたは親コンポーネントから受け取るpropsが変更されるたびに再レンダリングされます。不要な再レンダリングはパフォーマンスの低下に繋がるため、適切なメモ化が重要です。

#### 9.4.1 ロジックの外部化

コンポーネントのレンダリングに直接関係ない純粋な関数や定数は、コンポーネントの外部に定義することで、再レンダリングのたびに再生成されるのを防ぎます。

**悪い例:**
```typescript
// OrderListPage.tsx
const OrderListPage = () => {
  // レンダリングのたびに再生成される
  const formatCurrency = (amount: number) => { /* ... */ };
  const statusLabels = { pending: '待機中', /* ... */ };

  return (/* ... */);
}
```

**良い例:**
```typescript
// src/utils/orderUtils.ts (新しいファイル)
export const formatCurrency = (amount: number) => { /* ... */ };
export const statusLabels = { pending: '待機中', /* ... */ };

// OrderListPage.tsx
import { formatCurrency, statusLabels } from '@/utils/orderUtils';

const OrderListPage = () => {
  // 外部から一度だけインポートされる
  return (/* ... */);
}
```

#### 9.4.2 APIコール関数のメモ化 (`useMemo`)

データ取得フック（`useQuery`など）に渡すパラメータオブジェクトがレンダリングのたびに再生成されると、フックがパラメータの変更を検知し、不要なAPIリクエストを発生させる可能性があります。

**悪い例:**
```typescript
// OrderListPage.tsx
const OrderListPage = () => {
  const [statusFilter, setStatusFilter] = useState('all');

  // レンダリングのたびに新しいオブジェクトが生成される
  const { data } = useOrders(
    statusFilter !== 'all' ? { status: statusFilter } : undefined
  );

  return (/* ... */);
}
```

**良い例:**
```typescript
// OrderListPage.tsx
import { useMemo, useState } from 'react';

const OrderListPage = () => {
  const [statusFilter, setStatusFilter] = useState('all');

  // statusFilterが変更された時のみオブジェクトを再生成
  const queryParams = useMemo(() => {
    return statusFilter !== 'all' ? { status: statusFilter } : undefined;
  }, [statusFilter]);

  const { data } = useOrders(queryParams);

  return (/* ... */);
}
```

#### 9.4.3 イベントハンドラのメモ化 (`useCallback`)

（省略）

### 9.4.4 再利用可能なコンポーネントの防御的メモ化

`<DataTable>` のような再利用可能なコンポーネントを開発する際、親コンポーネントが `useCallback` を使わずにイベントハンドラを渡してくる可能性を考慮する必要があります。不安定な関数propは `useMemo` の依存配列に含まれていると、意図しない再計算を引き起こします。

この問題に対し、コンポーネント自身が「防御」するパターンが有効です。

**問題点:**
```typescript
// DataTable.tsx
const DataTable = ({ config, onAction }) => {
    const columns = useMemo(() => {
        // onAction が再生成されるたびに、この高コストな計算が再実行される
        return createColumns(config, onAction);
    }, [config, onAction]); // 👎 onActionに依存している

    return <Table columns={columns} />;
}
```

**解決策: `useRef` と `useEffect` で依存を分離する**

`useRef` を使って関数への最新の参照を保持し、`useMemo` の依存配列からその関数を削除します。

```typescript
// DataTable.tsx
import React, { useMemo, useRef, useEffect } from 'react';

const DataTable = ({ config, onAction }) => {
    // 1. onActionへの参照をuseRefで保持。refオブジェクトは常に同一
    const onActionRef = useRef(onAction);

    // 2. onActionが変更された場合のみ、refのcurrentプロパティを更新
    useEffect(() => {
        onActionRef.current = onAction;
    }, [onAction]);

    const columns = useMemo(() => {
        // 3. アクションカラムの定義では、ref経由で最新の関数を渡す
        const actionColumn = {
            id: 'actions',
            cell: ({ row }) => (
                <ActionMenu 
                    row={row.original}
                    onAction={onActionRef.current} // ✅ ref経由でアクセス
                />
            ),
        };
        return createColumns(config, actionColumn);

    }, [config]); // ✨ 依存配列からonActionを削除！

    return <Table columns={columns} />;
}
```
このパターンにより、`<DataTable>` は親コンポーネントの実装に影響されず、自身のパフォーマンスを安定して維持できます。

---

## 10. Gitワークフロー

### 10.1 ブランチ戦略

```bash
main         # 本番環境（常に安定）
  └─ develop      # 開発環境統合ブランチ
       ├─ feature/food-crud      # 機能開発
       ├─ feature/order-management
       ├─ bugfix/login-error     # バグ修正
       └─ hotfix/critical-bug    # 緊急修正
```

### 10.2 コミットメッセージ規約

```bash
# フォーマット: <type>: <subject>
# 
# type:
#   feat     新機能
#   fix      バグ修正
#   docs     ドキュメント
#   style    コードスタイル（機能変更なし）
#   refactor リファクタリング
#   test     テスト追加
#   chore    ビルド・設定変更

# 例:
git commit -m "feat: 商品一覧ページにカテゴリフィルタ追加"
git commit -m "fix: ログイン時のトークン保存エラーを修正"
git commit -m "docs: README.mdにセットアップ手順を追加"
git commit -m "refactor: DataTableコンポーネントをBarrel Export化"
```

### 10.3 プルリクエスト手順

```bash
# 1. 最新のdevelopを取得
git checkout develop
git pull origin develop

# 2. フィーチャーブランチを作成
git checkout -b feature/new-feature

# 3. 開発・コミット
git add .
git commit -m "feat: 新機能を追加"

# 4. リモートにプッシュ
git push origin feature/new-feature

# 5. GitHubでプルリクエスト作成
# - タイトル: [feat] 新機能を追加
# - 説明:
#   ## 変更内容
#   - 商品一覧ページを実装
#   - カテゴリフィルタを追加
#   
#   ## テスト方法
#   1. npm run dev で起動
#   2. /foods にアクセス
#   3. カテゴリフィルタが動作することを確認
#
#   ## スクリーンショット
#   （必要に応じて画像添付）

# 6. コードレビュー後、マージ
```

### 10.4 コードレビューチェックリスト

```markdown
## コードレビューチェックリスト

### 機能
- [ ] 要件を満たしているか
- [ ] エラーハンドリングは適切か
- [ ] エッジケースを考慮しているか

### コード品質
- [ ] 命名規則に従っているか
- [ ] TypeScriptの型定義は適切か
- [ ] コメントは適切に記述されているか
- [ ] 不要なコンソールログが残っていないか

### パフォーマンス
- [ ] 不要な再レンダリングはないか
- [ ] useMemo/useCallbackは適切に使用されているか
- [ ] TanStack QueryのstaleTimeは適切か

### セキュリティ
- [ ] XSS脆弱性はないか
- [ ] 認証・認可は適切に実装されているか
- [ ] 機密情報がログに出力されていないか

### テスト
- [ ] 単体テストは書かれているか（将来実装）
- [ ] 手動テストは完了しているか
```

---

## 📚 参考資料

- [React公式ドキュメント](https://react.dev/)
- [TypeScript公式ドキュメント](https://www.typescriptlang.org/)
- [TanStack Query公式ドキュメント](https://tanstack.com/query/latest)
- [Zustand公式ドキュメント](https://docs.pmnd.rs/zustand)
- [React Router v7ドキュメント](https://reactrouter.com/)

---

**最終更新**: 2025-10-15  
**バージョン**: 1.0.0
