# Node.js dependencies - 不复制到容器，在容器内重新安装
# Node.js依存関係 - コンテナにコピーしない、コンテナ内で再インストール
node_modules

# Build outputs - 开发环境不需要
# ビルド出力 - 開発環境では不要
dist
build

# Environment files - 安全考虑
# 環境ファイル - セキュリティ考慮
.env.local
.env.production

# IDE and OS files - 不需要在容器中
# IDEとOSファイル - コンテナ内不要
.vscode
.idea
.DS_Store
Thumbs.db

# Git files - 不需要
# Gitファイル - 不要
.git
.gitignore

# Logs - 不需要
# ログ - 不要
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Docker files - 避免递归复制
# Dockerファイル - 再帰コピー回避
Dockerfile
.dockerignore